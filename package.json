{
  "name": "ai-health-journal",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "packageManager": "npm@10.9.0",
  "engines": {
    "node": ">=18.17.0"
  },
  "devDependencies": {
    "prisma": "^5.22.0",
    "turbo": "^2.0.0",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "@types/bcryptjs": "^2.4.6",
    "bcryptjs": "^3.0.3",
    "ethers": "^6.16.0",
    "groq-sdk": "^1.1.1"
  }
}
